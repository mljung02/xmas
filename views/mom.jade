extends layout

block content
  div.row
    div.small-12.columns
      div What happened during the summer of 2014?
      form(method="post" action="/mom")
        input(type='text' name='guess')
        if error
          div=error
        button(type='submit') Confirm